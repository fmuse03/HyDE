## █▄▀ █▀▀ █▄█ █▄▄ █ █▄░█ █▀▄ █ █▄░█ █▀▀ █▀
## █░█ ██▄ ░█░ █▄█ █ █░▀█ █▄▀ █ █░▀█ █▄█ ▄█

# see https://wiki.hyprland.org/configuring/keywords/ for more
# example binds, see https://wiki.hyprland.org/configuring/binds/ for more

#? ------- KEYBINDS_HYPRLAND_V_0_53 COMPATIBLE ----------------
#   This Config is compatible with keybinding changes in Hyprland v0.53+
#   To ensure your system is working we will not disable this block
#   User template files will be provided in $XDG_DATA_HOME/hyde/templates/hypr/keybindings.conf for manual update.

#? ------- Grouping of binds for easier management ----------------
#  $d=[Group Name|Subgroup Name1|Subgroup Name2|...]
# '$d' is a variable that is used to group binds together (or use another variable)
# This is only for organization purposes and is not a defined hyprland variable
# What we did here is to modify the Description of the binds to include the group name
# The $d will be parsed as a separate key to be use for a GUI or something pretty
# [Main|Subgroup1|Subgroup2|...]
# Main - The main groupname
# Subgroup1.. - The subgroup names can be use to avoid repeating the same description

#? ------- Variables ----------------
# Default if commented out
# $mainMod = Super # super / meta / windows key
$launchMod = $mainMod shift
$killMod = $mainMod control shift
$focusMod = $mainMod
$ctrlMod = $mainMod control
# Assign apps
# $TERMINAL = kitty
# $EDITOR = code
# $EXPLORER = dolphin
# $BROWSER = firefox

$dgrp=System
$d=[$dgrp|Audio]
binddl  = , xf86audiomicmute,     $d un/mute microphone,  exec, hyde-shell volumecontrol -i m # toggle microphone mute
binddl  = , xf86audiomute,        $d toggle mute output,  exec, hyde-shell volumecontrol -o m # toggle audio mute
binddl  = , xf86audionext,        $d next media,          exec, playerctl next # media next
binddl  = , xf86audiopause,       $d pause media,         exec, playerctl play-pause # toggle between media play & pause
binddl  = , xf86audioplay,        $d play media,          exec, playerctl play-pause # toggle between media play & pause
binddl  = , xf86audioprev,        $d previous media,      exec, playerctl previous # media previous
binddle = , xf86audiolowervolume, $d decrease volume,     exec, hyde-shell volumecontrol -o d # decrease volume
binddle = , xf86audioraisevolume, $d increase volume,     exec, hyde-shell volumecontrol -o i # increase volume

$d=[$dgrp|Input]
bindd   = $mainMod alt, home,  mouse auto areas,   exec, wl-kbptr -o modes=floating -o mode_floating.source=detect
bindd   = $mainMod alt, next,  mouse split areas,  exec, wl-kbptr -o modes=split
bindd   = $mainMod,     home,  mouse left click,   exec, wlrctl pointer click left
bindd   = $mainMod,     next,  mouse right click,  exec, wlrctl pointer click right
bindde  = $mainMod alt, prior, mouse scroll left,  exec, wlrctl pointer scroll  0  -10
bindde  = $mainMod alt, end,   mouse scroll right, exec, wlrctl pointer scroll  0   10
bindde  = $mainMod,     prior, mouse scroll down,  exec, wlrctl pointer scroll  10  0
bindde  = $mainMod,     end,   mouse scroll up,    exec, wlrctl pointer scroll -10  0
bindde  = $mainMod,     down,  mouse down,         exec, wlrctl pointer move  0   10
bindde  = $mainMod,     left,  mouse left,         exec, wlrctl pointer move -10  0
bindde  = $mainMod,     right, mouse right,        exec, wlrctl pointer move  10  0
bindde  = $mainMod,     up,    mouse up,           exec, wlrctl pointer move  0  -10
# binddl  = $mainMod, K, $d toggle keyboard layout , exec, hyde-shell keyboardswitch # switch keyboard layout

$d=[$dgrp|Screen]
binddl  = alt, print,                 $d print all monitors,  exec, hyde-shell screenshot p # all monitors screenshot capture
binddle = alt, xf86monbrightnessdown, $d decrease gamma,      exec, hyde-shell hyprsunset --cm gamma -d 5 # decrease gamma
binddle = alt, xf86monbrightnessup,   $d increase gamma,      exec, hyde-shell hyprsunset --cm gamma -i 5 # increase gamma
binddl  = ,    print,                 $d print monitor,       exec, hyde-shell screenshot m # monitor screenshot capture
binddle = ,    xf86monbrightnessdown, $d decrease brightness, exec, hyde-shell brightnesscontrol d # decrease brightness
binddle = ,    xf86monbrightnessup,   $d increase brightness, exec, hyde-shell brightnesscontrol i # increase brightness

$d=[$dgrp|Global Keybinds]
$classDiscord=ArmCord|WebCord|com.discordapp.Discord|discord|vesktop
bindd   = $mainMod, M, $d un/mute, sendshortcut, CTRL SHIFT, M, class:^($classDiscord)$

$dgrp=Launch
$d=[$dgrp|App]
bindd   = $launchMod alt, P,      $d freeze & snip screen, exec,   hyde-shell screenshot sf # partial screenshot capture (frozen screen)
bindd   = $launchMod,     B,      $d web browser,            exec, $BROWSER
bindd   = $launchMod,     C,      $d color picker,           exec, hyprpicker -an # Pick color (Hex) >> clipboard#
bindd   = $launchMod,     E,      $d file explorer,          exec, $EXPLORER
bindd   = $launchMod,     G,      $d open game launcher,     exec, hyde-shell gamelauncher # run game launcher for steam & lutris
bindd   = $launchMod,     N,      $d text editor,            exec, $EDITOR
bindd   = $launchMod,     P,      $d snip screen,            exec, hyde-shell screenshot s # partial screenshot capture
bindd   = $launchMod,     escape, $d system monitor,         exec, hyde-shell system.monitor
bindd   = $launchMod,     grave,  $d terminal emulator,      exec, $TERMINAL

$d=[$dgrp|Menu]
bindd   = $launchMod alt, space,  $d runner,               exec, pkill -x rofi || hyde-shell rofilaunch r
bindd   = $launchMod,     E,      $d file finder,          exec, pkill -x rofi || hyde-shell rofilaunch f
bindd   = $launchMod,     L,      $d logout menu,          exec, hyde-shell logoutlaunch
bindd   = $launchMod,     V,      $d clipboard manager,    exec, pkill -x rofi || hyde-shell cliphist # launch clipboard Manager
bindd   = $launchMod,     comma,  $d emoji  picker,        exec, pkill -x rofi || hyde-shell emoji-picker # launch emoji picker
bindd   = $launchMod,     period, $d glyph picker,         exec, pkill -x rofi || hyde-shell glyph-picker # launch glyph picker
bindd   = $launchMod,     slash,  $d keybindings hint,     exec, pkill -x rofi || hyde-shell keybinds_hint c # launch keybinds hint
bindd   = $launchMod,     space,  $d application finder,   exec, pkill -x rofi || hyde-shell rofilaunch d
bindd   = $launchMod,     tab,    $d window switcher,      exec, pkill -x rofi || hyde-shell rofilaunch w
bindd   = $mainMod,       V,      $d clipboard,            exec, pkill -x rofi || hyde-shell cliphist -c # launch clipboard

$dgrp=Kill
$d=[$dgrp]
bindd   = $killMod alt, B,      $d toggle waybar & reload config, exec, hyde-shell waybar --hide
bindd   = $killMod,     B,      $d toggle waybar,                 exec, killall waybar || waybar # toggle waybar without reloading, this is faster
binddl  = $killMod,     D,      $d toggle dpms,                   dpms, toggle
bindd   = $killMod,     G,      $d toggle hypr effects,           exec, hyde-shell gamemode # disable hypr effects for gamemode
bindd   = $killMod,     L,      $d lock screen,                   exec, hyde-shell lock-session
bindd   = $killMod alt, L,      $d logout menu,                   exec, hyde-shell logoutlaunch
bindd   = $killMod,     Q,      $d kill focused window,           forcekillactive
bindd   = $killMod,     delete, $d kill hyprland session,         exec, hyde-shell logout

$dgrp=Focus
$d=[$dgrp]
bindd   = $focusMod alt, tab,        $d previous window,    cyclenext, prev
bindd   = $focusMod alt, down,       $d next monitor,       focusmonitor, +1
bindd   = $focusMod alt, left,       $d previous monitor,   focusmonitor, -1
bindd   = $focusMod alt, right,      $d next monitor,       focusmonitor, +1
bindd   = $focusMod alt, up,         $d previous monitor,   focusmonitor, -1
bindd   = $focusMod,     grave,      $d dropdown terminal,  exec, hyde-shell pypr toggle console
bindd   = $focusMod,     H,          $d focus left,         movefocus, l
bindd   = $focusMod,     J,          $d focus down,         movefocus, d
bindd   = $focusMod,     K,          $d focus up,           movefocus, u
bindd   = $focusMod,     L,          $d focus right,        movefocus, r
bindd   = $focusMod,     1,          $d workspace 1,        workspace, 1
bindd   = $focusMod,     2,          $d workspace 2,        workspace, 2
bindd   = $focusMod,     3,          $d workspace 3,        workspace, 3
bindd   = $focusMod,     4,          $d workspace 4,        workspace, 4
bindd   = $focusMod,     5,          $d special workspace,  togglespecialworkspace
bindd   = $focusMod,     0,          $d special workspace,  togglespecialworkspace
bindd   = $focusMod,     mouse_down, $d next workspace,     workspace, m+1
bindd   = $focusMod,     mouse_up,   $d previous workspace, workspace, m-1
bindd   = $focusMod,     tab,        $d cycle focus,        cyclenext

$dgrp=Control
$d=[$dgrp]
bindd   = $ctrlMod, J, $d toggle split,         togglesplit
bindd   = $ctrlMod, P, $d pin window,           exec, hyde-shell windowpin
bindd   = $ctrlMod, Q, $d close focused window, exec, hyde-shell dontkillsteam
bindd   = $ctrlMod, W, $d Toggle floating,      togglefloating

$d=[$dgrp|Move]
bindd   = $ctrlMod alt, 1,     $d move window to workspace 1 (silent),       movetoworkspacesilent, 1
bindd   = $ctrlMod alt, 2,     $d move window to workspace 2 (silent),       movetoworkspacesilent, 2
bindd   = $ctrlMod alt, 3,     $d move window to workspace 3 (silent),       movetoworkspacesilent, 3
bindd   = $ctrlMod alt, 4,     $d move window to workspace 4 (silent),       movetoworkspacesilent, 4
bindd   = $ctrlMod alt, 5,     $d move window to special workspace (silent), movetoworkspacesilent, special
bindd   = $ctrlMod alt, 0,     $d move window to special workspace (silent), movetoworkspacesilent, special
bindd   = $ctrlMod alt, down,  $d move workspace to next monitor,            movecurrentworkspacetomonitor, +1
bindd   = $ctrlMod alt, left,  $d move workspace to previous monitor,        movecurrentworkspacetomonitor, -1
bindd   = $ctrlMod alt, right, $d move workspace to next monitor,            movecurrentworkspacetomonitor, +1
bindd   = $ctrlMod alt, up,    $d move workspace to previous monitor,        movecurrentworkspacetomonitor, -1
bindd   = $ctrlMod,     1,     $d move window to workspace 1,                movetoworkspace, 1
bindd   = $ctrlMod,     2,     $d move window to workspace 2,                movetoworkspace, 2
bindd   = $ctrlMod,     3,     $d move window to workspace 3,                movetoworkspace, 3
bindd   = $ctrlMod,     4,     $d move window to workspace 4,                movetoworkspace, 4
bindd   = $ctrlMod,     5,     $d move window to special workspace,          movetoworkspace, special
bindd   = $ctrlMod,     0,     $d move window to special workspace,          movetoworkspace, special
$moveactivewindow=grep -q "true" <<< $(hyprctl activewindow -j | jq -r .floating) && hyprctl dispatch moveactive
bindde  = $ctrlMod,     H,     $d move window left,                          exec, $moveactivewindow -20  0  || hyprctl dispatch movewindow l
bindde  = $ctrlMod,     L,     $d move window right,                         exec, $moveactivewindow  20  0  || hyprctl dispatch movewindow r
bindde  = $ctrlMod,     K,     $d move window up,                            exec, $moveactivewindow  0  -20 || hyprctl dispatch movewindow u
bindde  = $ctrlMod,     J,     $d move window down,                          exec, $moveactivewindow  0   20 || hyprctl dispatch movewindow d

$d=[$dgrp|Resize]
bindd   = $ctrlMod alt, return,    $d fullscreen window,     fullscreen, 0
bindd   = $ctrlMod,     return,    $d maximize window,       fullscreen, 1
bindde  = $ctrlMod alt, H,         $d resize window left,    resizeactive, -20 0
bindde  = $ctrlMod alt, J,         $d resize window down,    resizeactive, 0 20
bindde  = $ctrlMod alt, K,         $d resize window up,      resizeactive, 0 -20
bindde  = $ctrlMod alt, L,         $d resize window right,   resizeactive, 20 0
binddm  = $ctrlMod,     mouse:272, $d hold to move window,   movewindow
binddm  = $ctrlMod,     mouse:273, $d hold to resize window, resizewindow

# TODO Make a main rofi menu for these selectors
$rice=Theming & Wallpaper
$d=[$rice]
# bindd   = $mainMod alt, Right, $d next global wallpaper , exec, hyde-shell wallpaper -Gn # next global wallpaper
# bindd   = $mainMod alt, Left, $d previous global wallpaper , exec, hyde-shell wallpaper -Gp # previous global wallpaper
# bindd   = $mainMod Shift, W, $d select a global wallpaper , exec, pkill -x rofi || hyde-shell wallpaper -SG # launch wallpaper select menu
# bindd   = $mainMod alt, Up, $d next waybar layout , exec, hyde-shell wbarconfgen n # next waybar mode
# bindd   = $mainMod alt, Down, $d previous waybar layout , exec, hyde-shell wbarconfgen p # previous waybar mode
# bindd   = $mainMod Shift, R, $d wallbash mode selector , exec, pkill -x rofi || hyde-shell wallbashtoggle -m # launch wallbash mode select menu
# bindd   = $mainMod Shift, T, $d select a theme, exec, pkill -x rofi || hyde-shell themeselect # launch theme select menu
# bindd   = $mainMod+Shift, Y, $d select animations, exec, pkill -x rofi || hyde-shell animations --select # launch animations select menu
# bindd   = $mainMod+Shift, U, $d select hyprlock layout, exec, pkill -x rofi || hyde-shell hyprlock --select # launch hyprlock layout select menu
# bindd   = $launchMod alt, A,      $d select rofi launcher, exec, pkill -x rofi || hyde-shell rofiselect # launch select menu

$d=#! unset the group name
